<h4>
	You are successfully logged in as <a href="profile.html"><span
		class="badge badge-primary">${sessionScope.student.firstName} ${sessionScope.student.lastName} (${sessionScope.student.username})</span></a> -
	<span class="badge badge-dark"><a href="signout.html">Sign
			Out</a></span>
</h4>
<br />